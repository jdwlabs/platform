repos:
  - repo: local
    hooks:
      - id: validate-tenants
        name: Validate Tenant YAMLs
        entry: platformctl tenants validate
        language: system
        files: ^tenants/.*tenant\.yaml$
        pass_filenames: false
