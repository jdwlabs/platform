apiVersion: db.atlasgo.io/v1alpha1
kind: AtlasSchema
metadata:
  name: dotablaze-tech-atlas-schema-prd
  annotations:
    argocd.argoproj.io/sync-wave: "3"
spec:
  credentials:
    scheme: postgres
    host: postgresql-cluster-prd-rw.database.svc
    user: postgres
    passwordFrom:
      secretKeyRef:
        key: password
        name: postgresql-cluster-prd-superuser
    database: dotablazetech_prd
    port: 5432
    parameters:
      sslmode: disable
  schema:
    configMapKeyRef:
      key: schema.sql
      name: dotablaze-tech-postgresql-prd-schema
