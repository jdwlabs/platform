datasources:
  datasources.yaml:
    datasources:
      - name: Prometheus
        type: prometheus
        access: proxy
        url: http://platform-prometheus-server:9090
        isDefault: true
      - name: Loki
        type: loki
        access: proxy
        url: http://platform-loki:3100

persistence:
  enabled: true
  storageClassName: ""
  accessModes:
    - ReadWriteOnce
  size: 2Gi
