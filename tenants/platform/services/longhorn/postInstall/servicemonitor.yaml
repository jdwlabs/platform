apiVersion: monitoring.coreos.com/v1
kind: Servicemonitor
metadata:
  name: longhorn-metrics
  namespace: longhorn-system
spec:
  selector:
    matchLabels:
      app: longhorn-maanger
  namespaceSelector:
    matchNames:
      - longhorn-system
  endpoints:
    - port: manager
      interval: 30s
      path: /metrics
