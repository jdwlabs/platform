# The chained CNI configuration Cilium writes to /etc/cni/net.d. The flannel
# and portmap plugin blocks and the `name` field reproduce kube-flannel-cfg's
# cni-conf.json exactly — CNI matches a conflist by `name`, so a value other
# than cbr0 yields a configuration nothing ever uses, with no error. Compare
# this against kube-flannel-cfg at every Flannel bump.
#
# chaining-mode belongs to the cilium-cni plugin block and is not the same
# setting as the daemon's cni-chaining-mode. The plugin binary reads only this
# file; without the key it sees flannel's PrevResult, concludes it was invoked
# unchained, and fails every pod sandbox creation on the node.
apiVersion: v1
kind: ConfigMap
metadata:
  name: cni-configuration
  namespace: kube-system
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
data:
  cni-config: |-
    {
      "name": "cbr0",
      "cniVersion": "1.0.0",
      "plugins": [
        {
          "type": "flannel",
          "delegate": {
            "hairpinMode": true,
            "isDefaultGateway": true
          }
        },
        {
          "type": "portmap",
          "capabilities": {
            "portMappings": true
          }
        },
        {
          "type": "cilium-cni",
          "chaining-mode": "generic-veth"
        }
      ]
    }
