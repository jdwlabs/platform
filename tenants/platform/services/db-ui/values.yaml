# The in-house chart's defaults (helm-charts/db-ui/values.yaml) carry the
# resource bounds and probes. Put platform-specific overrides here.

# ArgoCD holds prior manifests in git history; no need for k8s to keep old
# ReplicaSets around too.
revisionHistoryLimit: 0
