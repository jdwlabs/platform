apiVersion: gateway.nginx.org/v1alpha1
kind: AuthenticationFilter
metadata:
  name: openclaw-auth
  namespace: jdwlabs-ai
spec:
  type: Basic
  basic:
    realm: "Authentication Required - OpenClaw"
    secretRef:
      name: openclaw-basic-auth
