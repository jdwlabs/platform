apiVersion: batch/v1
kind: Job
metadata:
  name: flyway-jdwlabs-non-v2
  annotations:
    argocd.argoproj.io/hook: Sync
    argocd.argoproj.io/hook-delete-policy: BeforeHookCreation
    argocd.argoproj.io/sync-wave: "3"
spec:
  backoffLimit: 2
  ttlSecondsAfterFinished: 300
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: flyway
          image: flyway/flyway:11.2
          env:
            - name: FLYWAY_URL
              value: "jdbc:postgresql://platform-postgresql-cluster-non-rw.database.svc:5432/jdwlabs_non"
            - name: FLYWAY_USER
              value: postgres
            - name: FLYWAY_PASSWORD
              valueFrom:
                secretKeyRef:
                  key: password
                  name: platform-postgresql-cluster-non-superuser
            - name: FLYWAY_BASELINE_ON_MIGRATE
              value: "true"
            - name: FLYWAY_BASELINE_VERSION
              value: "0"
            - name: FLYWAY_DEFAULT_SCHEMA
              value: public
            - name: FLYWAY_SCHEMAS
              value: public,auth
          args:
            - "-locations=filesystem:/flyway/sql"
            - "repair"
            - "migrate"
          volumeMounts:
            - mountPath: /flyway/sql
              name: migrations
      volumes:
        - name: migrations
          configMap:
            name: jdwlabs-migrations
