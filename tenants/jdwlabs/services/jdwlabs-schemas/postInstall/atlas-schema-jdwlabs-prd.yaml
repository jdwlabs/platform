apiVersion: db.atlasgo.io/v1alpha1
kind: AtlasSchema
metadata:
  name: jdwlabs-atlas-schema-prd
  annotations:
    argocd.argoproj.io/sync-wave: "3"
spec:
  credentials:
    scheme: postgres
    host: platform-postgresql-cluster-prd-rw.database.svc
    user: app
    passwordFrom:
      secretKeyRef:
        key: password
        name: platform-postgresql-cluster-prd-app
    database: jdwlabs_prd
    port: 5432
    parameters:
      sslmode: disable
  schema:
    configMapKeyRef:
      key: schema.sql
      name: jdwlabs-postgresql-prd-schema
