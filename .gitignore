.idea/
vault-init.json
.claude/settings.local.json
cli/bin/
cli/dist/
platformctl
platformctl.exe
docs/superpowers/
.claude/worktrees/
worktrees/
.worktrees/
.secrets/

# Secrets — local credentials must never be committed
.env*
*.key
*.pem
kubeconfig*
*.kubeconfig
# Go sources named kubeconfig*.go (e.g. in cli/) are code, not credentials
!kubeconfig*.go
